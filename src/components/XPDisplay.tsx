import React from "react";

// XPDisplay is now replaced by GameHUD — this component is kept for backwards compatibility
// but renders nothing. GameHUD handles all XP/Level/Coin display.
const XPDisplay: React.FC = () => {
    return null;
};

export default XPDisplay;
